diff --git a/frontend/public/index.html b/frontend/public/index.html
index 274c230..6b6af73 100644
--- a/frontend/public/index.html
+++ b/frontend/public/index.html
@@ -3,6 +3,12 @@
   <head>
     <meta charset="utf-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1" />
+    <link rel="preconnect" href="https://fonts.googleapis.com" />
+    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
+    <link
+      href="https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,400;8..60,500;8..60,600;8..60,700&family=IBM+Plex+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap"
+      rel="stylesheet"
+    />
     <title>PersonaMatch</title>
   </head>
   <body>
diff --git a/frontend/src/App.css b/frontend/src/App.css
index c978052..7de3c1e 100644
--- a/frontend/src/App.css
+++ b/frontend/src/App.css
@@ -1,3 +1,5 @@
+/* ---------- layout shell ---------- */
+
 .app-shell {
   min-height: 100vh;
   display: flex;
@@ -8,144 +10,284 @@
   display: flex;
   justify-content: space-between;
   align-items: center;
-  padding: 14px 32px;
-  background: #ffffff;
-  border-bottom: 1px solid #e6e3f2;
+  padding: 18px 32px;
+  background: var(--paper);
+  border-bottom: 1px solid var(--rule);
 }
 
 .navbar .brand {
-  font-weight: 700;
-  font-size: 1.2rem;
-  color: #5b3df0;
+  display: inline-flex;
+  align-items: center;
+  gap: 10px;
+  font-family: var(--font-display);
+  font-weight: 600;
+  font-size: 1.25rem;
+  color: var(--ink);
+  letter-spacing: -0.01em;
+}
+
+.logomark {
+  flex: none;
+  display: block;
 }
 
 .nav-links {
   display: flex;
   align-items: center;
-  gap: 18px;
+  gap: 22px;
+  font-size: 0.92rem;
+}
+
+.nav-links a {
+  color: var(--ink-soft);
+  border-bottom: 1px solid transparent;
+  padding-bottom: 2px;
+}
+
+.nav-links a:hover {
+  color: var(--ink);
+  border-bottom-color: var(--coral);
 }
 
 .nav-user {
-  color: #706c85;
-  font-size: 0.9rem;
+  color: var(--muted);
+  font-family: var(--font-mono);
+  font-size: 0.82rem;
 }
 
 .link-btn {
   background: none;
   border: none;
-  color: #5b3df0;
+  color: var(--ink-soft);
   cursor: pointer;
   font: inherit;
   padding: 0;
 }
 
+.link-btn:hover {
+  color: var(--coral-dark);
+}
+
 .app-content {
   flex: 1;
-  padding: 32px;
-  max-width: 720px;
+  padding: 48px 32px 64px;
+  max-width: 640px;
   margin: 0 auto;
   width: 100%;
 }
 
+/* ---------- typography ---------- */
+
 .page h1 {
-  font-size: 2rem;
-  margin-bottom: 6px;
+  font-family: var(--font-display);
+  font-size: 2.5rem;
+  font-weight: 600;
+  line-height: 1.15;
+  margin: 0 0 14px;
+  letter-spacing: -0.01em;
+}
+
+.page h2 {
+  font-family: var(--font-display);
+  font-size: 1.6rem;
+  font-weight: 600;
+  margin: 0 0 18px;
 }
 
 .subtitle {
-  color: #6a6680;
-  line-height: 1.5;
+  color: var(--ink-soft);
+  line-height: 1.6;
+  font-size: 1.02rem;
+  max-width: 46ch;
+}
+
+/* ---------- hero (Home) ---------- */
+
+.hero-row {
+  display: flex;
+  align-items: center;
+  gap: 32px;
+  margin-bottom: 8px;
+}
+
+.hero-copy {
+  flex: 1;
+  min-width: 0;
+}
+
+.hero-venn {
+  flex: none;
+  width: 148px;
+  height: 148px;
+}
+
+@media (max-width: 640px) {
+  .hero-row {
+    flex-direction: column-reverse;
+    align-items: flex-start;
+    gap: 20px;
+  }
+  .hero-venn {
+    width: 108px;
+    height: 108px;
+  }
 }
 
 .cta-row {
   display: flex;
+  flex-wrap: wrap;
   gap: 12px;
-  margin: 24px 0;
+  margin: 28px 0 8px;
 }
 
+/* ---------- buttons ---------- */
+
 .btn {
-  display: inline-block;
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
   padding: 10px 18px;
-  border-radius: 8px;
-  border: 1px solid #ddd8ef;
-  background: #fff;
-  color: #23212b;
+  border-radius: 4px;
+  border: 1px solid var(--ink);
+  background: var(--paper-raised);
+  color: var(--ink);
   cursor: pointer;
+  font-family: var(--font-body);
   font-size: 0.95rem;
+  font-weight: 500;
+  transition: transform 0.1s ease, background 0.15s ease;
+}
+
+.btn:hover {
+  background: var(--ink);
+  color: var(--paper);
+}
+
+.btn:active {
+  transform: translateY(1px);
 }
 
 .btn.primary {
-  background: #5b3df0;
-  border-color: #5b3df0;
-  color: #fff;
+  background: var(--coral);
+  border-color: var(--coral);
+  color: #fff5f3;
+}
+
+.btn.primary:hover {
+  background: var(--coral-dark);
+  border-color: var(--coral-dark);
+  color: #fff5f3;
 }
 
 .btn.primary:disabled {
-  opacity: 0.6;
+  opacity: 0.55;
   cursor: default;
+  transform: none;
 }
 
+/* ---------- how it works ---------- */
+
 .how-it-works {
-  margin-top: 40px;
-  background: #fff;
-  border: 1px solid #e6e3f2;
-  border-radius: 10px;
-  padding: 20px 24px;
+  margin-top: 44px;
+  padding-top: 28px;
+  border-top: 1px solid var(--rule);
+}
+
+.how-it-works h3 {
+  font-family: var(--font-display);
+  font-weight: 600;
+  font-size: 1.1rem;
+  margin: 0 0 14px;
 }
 
 .how-it-works ol {
-  padding-left: 18px;
-  line-height: 1.7;
+  padding-left: 22px;
+  line-height: 1.8;
+  color: var(--ink-soft);
+  counter-reset: steps;
+}
+
+.how-it-works li {
+  padding-left: 4px;
+}
+
+.how-it-works li::marker {
+  font-family: var(--font-mono);
+  color: var(--mustard);
+  font-weight: 500;
 }
 
+/* ---------- index cards ---------- */
+/* the recurring "library card" surface: hairline border, clipped corner,
+   no drop shadow. used for auth forms and match results alike. */
+
 .card {
-  background: #fff;
-  border: 1px solid #e6e3f2;
-  border-radius: 10px;
-  padding: 24px;
+  background: var(--paper-raised);
+  border: 1px solid var(--rule);
+  clip-path: polygon(0 0, calc(100% - 18px) 0, 100% 18px, 100% 100%, 0 100%);
+  padding: 26px 26px 22px;
   display: flex;
   flex-direction: column;
   gap: 4px;
-  max-width: 420px;
+  max-width: 440px;
 }
 
 .card label {
-  margin-top: 12px;
-  font-size: 0.85rem;
-  color: #6a6680;
+  margin-top: 14px;
+  font-size: 0.82rem;
+  color: var(--muted);
+  font-family: var(--font-mono);
 }
 
 .card input,
 .card textarea,
 .card select {
-  padding: 9px 10px;
-  border: 1px solid #d8d4ea;
-  border-radius: 6px;
+  padding: 10px 11px;
+  border: 1px solid var(--rule);
+  border-radius: 3px;
   font-size: 0.95rem;
-  font-family: inherit;
+  font-family: var(--font-body);
+  background: var(--paper);
+  color: var(--ink);
+}
+
+.card input:focus,
+.card textarea:focus,
+.card select:focus {
+  border-color: var(--ink);
 }
 
 .card button {
-  margin-top: 18px;
+  margin-top: 20px;
+  align-self: flex-start;
 }
 
 .error-text {
-  color: #cc3355;
+  color: var(--coral-dark);
   font-size: 0.85rem;
-  margin: 6px 0 0;
+  margin: 8px 0 0;
+  font-family: var(--font-mono);
 }
 
 .status-text {
-  color: #2f9e60;
+  color: var(--mustard);
   font-size: 0.85rem;
   margin-left: 10px;
+  font-family: var(--font-mono);
+}
+
+.auth-page p {
+  color: var(--ink-soft);
+  margin-top: 18px;
 }
 
+/* ---------- matches ---------- */
+
 .match-list {
   margin-top: 20px;
   display: flex;
   flex-direction: column;
-  gap: 14px;
+  gap: 16px;
 }
 
 .match-card {
@@ -155,41 +297,79 @@
 .match-header {
   display: flex;
   justify-content: space-between;
-  align-items: center;
+  align-items: flex-start;
+  gap: 12px;
 }
 
-.score-pill {
-  background: #ece8fc;
-  color: #5b3df0;
-  font-size: 0.8rem;
-  padding: 3px 10px;
-  border-radius: 999px;
+.match-header strong {
+  font-family: var(--font-display);
+  font-size: 1.15rem;
+  font-weight: 600;
+}
+
+.score-stamp {
+  flex: none;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  width: 52px;
+  height: 52px;
+  border-radius: 50%;
+  border: 1.5px solid var(--coral);
+  color: var(--coral-dark);
+  font-family: var(--font-mono);
+  font-size: 0.78rem;
+  font-weight: 500;
+  transform: rotate(-6deg);
 }
 
 .match-card .bio {
-  color: #4d4a5c;
-  margin: 8px 0;
+  color: var(--ink-soft);
+  margin: 10px 0;
+  line-height: 1.55;
 }
 
-.match-card .tags {
-  color: #8b87a0;
-  font-size: 0.85rem;
-  margin-bottom: 12px;
+.tag-list {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 6px;
+  margin: 4px 0 14px;
+}
+
+.tag-chip {
+  font-family: var(--font-mono);
+  font-size: 0.75rem;
+  color: var(--mustard);
+  border: 1px solid var(--mustard);
+  border-radius: 3px;
+  padding: 2px 8px;
 }
 
+select {
+  padding: 9px 10px;
+  border: 1px solid var(--rule);
+  border-radius: 3px;
+  background: var(--paper-raised);
+  font-family: var(--font-body);
+  color: var(--ink);
+  margin: 6px 0 20px;
+  display: block;
+}
+
+/* ---------- chat ---------- */
+
 .chat-page {
   display: flex;
   flex-direction: column;
-  height: calc(100vh - 160px);
+  height: calc(100vh - 200px);
 }
 
 .chat-window {
   flex: 1;
   overflow-y: auto;
-  background: #fff;
-  border: 1px solid #e6e3f2;
-  border-radius: 10px;
-  padding: 16px;
+  background: var(--paper-raised);
+  border: 1px solid var(--rule);
+  padding: 18px;
   display: flex;
   flex-direction: column;
   gap: 8px;
@@ -197,22 +377,25 @@
 
 .chat-bubble {
   max-width: 70%;
-  padding: 8px 12px;
-  border-radius: 12px;
-  font-size: 0.9rem;
-  line-height: 1.4;
+  padding: 9px 13px;
+  border-radius: 10px;
+  font-size: 0.92rem;
+  line-height: 1.45;
 }
 
 .chat-bubble.mine {
   align-self: flex-end;
-  background: #5b3df0;
-  color: #fff;
+  background: var(--ink);
+  color: var(--paper);
+  border-bottom-right-radius: 2px;
 }
 
 .chat-bubble.theirs {
   align-self: flex-start;
-  background: #f0eef9;
-  color: #23212b;
+  background: var(--paper);
+  border: 1px solid var(--rule);
+  color: var(--ink);
+  border-bottom-left-radius: 2px;
 }
 
 .chat-input-row {
@@ -223,7 +406,14 @@
 
 .chat-input-row input {
   flex: 1;
-  padding: 10px 12px;
-  border-radius: 8px;
-  border: 1px solid #d8d4ea;
+  padding: 11px 13px;
+  border-radius: 3px;
+  border: 1px solid var(--rule);
+  font-family: var(--font-body);
+  background: var(--paper-raised);
+  color: var(--ink);
+}
+
+.chat-input-row input:focus {
+  border-color: var(--ink);
 }
diff --git a/frontend/src/components/Navbar.js b/frontend/src/components/Navbar.js
index c0fe332..33e1bcc 100644
--- a/frontend/src/components/Navbar.js
+++ b/frontend/src/components/Navbar.js
@@ -12,7 +12,13 @@ export default function Navbar({ user, setUser }) {
 
   return (
     <nav className="navbar">
-      <Link to="/" className="brand">PersonaMatch</Link>
+      <Link to="/" className="brand">
+        <svg className="logomark" width="26" height="20" viewBox="0 0 26 20" aria-hidden="true">
+          <circle cx="10" cy="10" r="8.5" fill="none" stroke="#1c2340" strokeWidth="1.5" />
+          <circle cx="17" cy="10" r="8.5" fill="none" stroke="#ff5a52" strokeWidth="1.5" />
+        </svg>
+        PersonaMatch
+      </Link>
 
       {user ? (
         <div className="nav-links">
diff --git a/frontend/src/index.css b/frontend/src/index.css
index 2b35df3..c455fb1 100644
--- a/frontend/src/index.css
+++ b/frontend/src/index.css
@@ -2,14 +2,41 @@
   box-sizing: border-box;
 }
 
+:root {
+  --ink: #1c2340;
+  --ink-soft: #4a4f6b;
+  --muted: #857f6e;
+  --paper: #faf8f3;
+  --paper-raised: #ffffff;
+  --rule: #dedad0;
+  --coral: #ff5a52;
+  --coral-dark: #e0453e;
+  --mustard: #c98a2c;
+  --font-display: "Source Serif 4", Georgia, serif;
+  --font-body: "IBM Plex Sans", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+  --font-mono: "IBM Plex Mono", "Courier New", monospace;
+}
+
 body {
   margin: 0;
-  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
-  background: #f6f5fb;
-  color: #23212b;
+  font-family: var(--font-body);
+  background: var(--paper);
+  color: var(--ink);
 }
 
 a {
-  color: #5b3df0;
+  color: var(--ink);
   text-decoration: none;
 }
+
+:focus-visible {
+  outline: 2px solid var(--coral);
+  outline-offset: 2px;
+}
+
+@media (prefers-reduced-motion: reduce) {
+  * {
+    animation-duration: 0.01ms !important;
+    transition-duration: 0.01ms !important;
+  }
+}
diff --git a/frontend/src/pages/Home.js b/frontend/src/pages/Home.js
index 18c9330..9cfdbd6 100644
--- a/frontend/src/pages/Home.js
+++ b/frontend/src/pages/Home.js
@@ -4,11 +4,22 @@ import { Link } from "react-router-dom";
 export default function Home({ user }) {
   return (
     <div className="page home">
-      <h1>PersonaMatch</h1>
-      <p className="subtitle">
-        Find the right study partner, project teammate, roommate or mentor on campus -
-        matched on what actually matters, not just who's online.
-      </p>
+      <div className="hero-row">
+        <div className="hero-copy">
+          <h1>PersonaMatch</h1>
+          <p className="subtitle">
+            Find the right study partner, project teammate, roommate or mentor on campus -
+            matched on what actually matters, not just who's online.
+          </p>
+        </div>
+        <svg className="hero-venn" viewBox="0 0 160 160" aria-hidden="true">
+          <circle cx="62" cy="80" r="52" fill="none" stroke="#1c2340" strokeWidth="1.5" />
+          <circle cx="98" cy="80" r="52" fill="none" stroke="#ff5a52" strokeWidth="1.5" />
+          <text x="80" y="85" textAnchor="middle" fontFamily="IBM Plex Mono, monospace" fontSize="11" fill="#c98a2c">
+            match
+          </text>
+        </svg>
+      </div>
 
       {!user && (
         <div className="cta-row">
diff --git a/frontend/src/pages/Matches.js b/frontend/src/pages/Matches.js
index 2d1daad..399c0dc 100644
--- a/frontend/src/pages/Matches.js
+++ b/frontend/src/pages/Matches.js
@@ -50,10 +50,14 @@ export default function Matches({ user }) {
           <div key={m.user_id} className="card match-card">
             <div className="match-header">
               <strong>{m.name}</strong>
-              <span className="score-pill">{Math.round(m.score * 100)}% match</span>
+              <span className="score-stamp">{Math.round(m.score * 100)}%</span>
             </div>
             {m.bio && <p className="bio">{m.bio}</p>}
-            <p className="tags">{m.tags.join(", ")}</p>
+            <div className="tag-list">
+              {m.tags.map((t) => (
+                <span key={t} className="tag-chip">{t}</span>
+              ))}
+            </div>
             <Link to={`/chat/${m.user_id}/${encodeURIComponent(m.name)}`} className="btn">
               Message
             </Link>
